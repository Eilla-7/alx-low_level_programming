#include<ctype.h>
#include<stdio.h>
#include "main.h"

/**
 * mul - is a function to multiplay toe integers
 * Return: the result of the operation
 * @a: the first number
 * @b: the secound number
 */

int mul(int a, int b)
{
int result = a / b;
return (result);
}
