#include<stdio.h>
#include "main.h"

/**
 * print_numbers - a function to print numbers
 * Return: it doesn't return any value
 * @void: it doesn't have a parameter
 */

void print_numbers(void)
{
int i;
for (i = 0; i < 10; i++)
{
printf("%d", i);
}
return;
}
